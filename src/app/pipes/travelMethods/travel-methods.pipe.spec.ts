import { TravelMethodsPipe } from './travel-methods.pipe';

describe('TravelMethodsPipe', () => {
  it('create an instance', () => {
    const pipe = new TravelMethodsPipe();
    expect(pipe).toBeTruthy();
  });
});
